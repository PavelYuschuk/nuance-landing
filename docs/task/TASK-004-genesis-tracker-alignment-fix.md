# TASK-004: Уточнение Индикации и Выравнивания «3.3. Генезис / О нас»

**Status:** DONE  
**Target File(s):** `index.html`, `src/styles/genesis.css`, `src/scripts/about-scroll.js`  
**Parent Specification:** TASK-003 (`docs/task/TASK-003-genesis-section-update.md`)

---

## 1. Контекст и Цель Модификации
Доработка логики индикатора прогресса и геометрии блока «3.3. Генезис / О нас»:
1. Перевод прогресс-бара с непрерывного скролл-трекинга на пошаговую дискретную привязку к активному слою (1/3 ➔ 2/3 ➔ 3/3).
2. Выравнивание шапки секции (`[О нас]` + монолитная линия) строго по верхнему левому краю текстовой колонки (`col-5`).
3. Корректировка заголовка первого экрана (удаление слова `NUANCE.`).

Все остальные параметры из TASK-003 сохраняются **без изменений**.

---

## 2. Инженерная Спецификация (Design Spec)

### 2.1. Дискретная Логика Заполнения Прогресс-бара (`.about-progress-tracker`)
- **Поведение линии заполнения (`.about-progress-fill` / `.tracker-progress-fill`):**
  - Заполнение привязано к переключению активного слоя (`data-layer-id`):
    - **Слой 0 (Кто мы):** `width: 33.33%` (1/3).
    - **Слой 1 (История):** `width: 66.66%` (2/3).
    - **Слой 2 (Механизм):** `width: 100%` (3/3).
  - Плавность анимации перехода при смене слоя:  
    `transition: width 0.4s cubic-bezier(0.16, 1, 0.3, 1)`.
- **Изменения в JS (`src/scripts/about-scroll.js`):**
  - При срабатывании триггера переключения активного слоя `activeLayerIndex` обновлять ширину полосы прогресса:
    ```javascript
    const fillPercent = ((activeLayerIndex + 1) / totalLayers) * 100;
    progressFillElement.style.width = `${fillPercent}%`;
    ```

---

### 2.2. Позиционирование и Выравнивание Шапки Секции
- **Родительский контейнер (`.about-header-subblock`):**
  - Отказ от независимого/вынесенного центрирования.
  - Позиционирование: Внутри текстовой колонки (`col-5`) над основным подзаголовком H2 ИЛИ совмещенное выравнивание по левой сетке `col-5` (`margin-left: 0; padding-left: 0; align-self: flex-start`).
  - `text-align: left`.
- **Заголовок `[О нас]` (`.about-main-title`):**
  - Выравнивание строго по левой линии сетки колонок контента (`col-5`).
  - `margin-bottom: 8px`.
- **Линия прогресса (`.about-progress-tracker`):**
  - Расположена непосредственно под надписью `[О нас]`.
  - `width: 100%; max-width: 280px` (вписывается в ширину текстового блока `col-5`).

---

### 2.3. Корректировка Копирайтинга (Слой 0)
- **Заголовок H2 (`.about-sub-title` на Экране 1):**
  - **Было:** `NUANCE. Асимметрия как стандарт.`
  - **Стало:** `Асимметрия как стандарт.`
- **Текст P (`.genesis-paragraph`):**
  `Там, где остальные упираются в предсказуемые лобовые штурмы, мы заходим в глубокий тыл. Мы не строим иллюзий — идеальные планы живут ровно до первого контакта с противником. Наш профиль: контролируемый хаос и агрессивная импровизация. Любая стратегия врага неизбежно споткнется о непредвиденный нюанс. И этот нюанс — мы.`

---

## 3. Чек-лист проверки выполнения (Verification)
- [ ] На Слое 0 полоса заполнения прогресс-бара сразу установлена на 33.3% (1/3).
- [ ] При переходе на Слой 1 линия плавно дорастает до 66.6% (2/3).
- [ ] При переходе на Слой 2 линия заполняется до 100% (3/3).
- [ ] Заголовок `[О нас]` и прогресс-бар выровнены по левому краю текстовой зоны `col-5`.
- [ ] Заголовок первого подблока обновлен на `Асимметрия как стандарт.`.
